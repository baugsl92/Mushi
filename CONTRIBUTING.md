# Contributing

Use GitHub's web editor, a local clone, or any editor you prefer.

1. Create a branch.
2. Make the change.
3. Run `python -m pytest -q` when working locally.
4. Open a pull request.

Do not commit `.streamlit/secrets.toml`, ntfy topics, tokens, exact sensitive mushroom locations, SQLite files, or generated alert state.
